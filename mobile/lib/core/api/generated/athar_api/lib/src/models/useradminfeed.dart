// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'useradminread.dart';

/// Object model for `UserAdminFeed` from the SumUp API spec.
class UserAdminFeed {
  const UserAdminFeed({
    required this.items,
    required this.total,
    required this.page,
    required this.pageSize,
    required this.totalPages,
    required this.hasPrev,
    required this.hasNext,
  });

  final List<UserAdminRead> items;
  final int total;
  final int page;
  final int pageSize;
  final int totalPages;
  final bool hasPrev;
  final bool hasNext;

  /// Creates an instance from a JSON map.
  factory UserAdminFeed.fromJson(Map<String, dynamic> json) {
    return UserAdminFeed(
      items: List<UserAdminRead>.generate(
          (json['items'] as List).length,
          (i) => UserAdminRead.fromJson(
              (json['items'] as List)[i] as Map<String, dynamic>),
          growable: false),
      total: (json['total'] as num).toInt(),
      page: (json['page'] as num).toInt(),
      pageSize: (json['page_size'] as num).toInt(),
      totalPages: (json['total_pages'] as num).toInt(),
      hasPrev: json['has_prev'] as bool,
      hasNext: json['has_next'] as bool,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'items': items.map((e) => e?.toJson()).toList(),
      'total': total,
      'page': page,
      'page_size': pageSize,
      'total_pages': totalPages,
      'has_prev': hasPrev,
      'has_next': hasNext,
    };
  }

  UserAdminFeed copyWith({
    List<UserAdminRead>? items,
    int? total,
    int? page,
    int? pageSize,
    int? totalPages,
    bool? hasPrev,
    bool? hasNext,
  }) {
    if (items == null &&
        total == null &&
        page == null &&
        pageSize == null &&
        totalPages == null &&
        hasPrev == null &&
        hasNext == null) {
      return this;
    }

    return UserAdminFeed(
      items: items ?? this.items,
      total: total ?? this.total,
      page: page ?? this.page,
      pageSize: pageSize ?? this.pageSize,
      totalPages: totalPages ?? this.totalPages,
      hasPrev: hasPrev ?? this.hasPrev,
      hasNext: hasNext ?? this.hasNext,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! UserAdminFeed) {
      return false;
    }
    return items == other.items &&
        total == other.total &&
        page == other.page &&
        pageSize == other.pageSize &&
        totalPages == other.totalPages &&
        hasPrev == other.hasPrev &&
        hasNext == other.hasNext;
  }

  @override
  int get hashCode =>
      Object.hash(items, total, page, pageSize, totalPages, hasPrev, hasNext);

  @override
  String toString() =>
      'UserAdminFeed(items=$items, total=$total, page=$page, pageSize=$pageSize, totalPages=$totalPages, hasPrev=$hasPrev, hasNext=$hasNext)';
}
