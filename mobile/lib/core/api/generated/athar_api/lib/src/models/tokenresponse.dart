// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'userread.dart';

/// Object model for `TokenResponse` from the SumUp API spec.
class TokenResponse {
  const TokenResponse({
    required this.accessToken,
    required this.refreshToken,
    this.tokenType,
    required this.user,
  });

  final String accessToken;
  final String refreshToken;
  final String? tokenType;
  final UserRead user;

  /// Creates an instance from a JSON map.
  factory TokenResponse.fromJson(Map<String, dynamic> json) {
    return TokenResponse(
      accessToken: json['access_token'] as String,
      refreshToken: json['refresh_token'] as String,
      tokenType:
          json['token_type'] != null ? json['token_type'] as String : null,
      user: UserRead.fromJson(json['user'] as Map<String, dynamic>),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'access_token': accessToken,
      'refresh_token': refreshToken,
      if (tokenType != null) 'token_type': tokenType!,
      'user': user?.toJson(),
    };
  }

  TokenResponse copyWith({
    String? accessToken,
    String? refreshToken,
    String? tokenType,
    UserRead? user,
  }) {
    if (accessToken == null &&
        refreshToken == null &&
        tokenType == null &&
        user == null) {
      return this;
    }

    return TokenResponse(
      accessToken: accessToken ?? this.accessToken,
      refreshToken: refreshToken ?? this.refreshToken,
      tokenType: tokenType ?? this.tokenType,
      user: user ?? this.user,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! TokenResponse) {
      return false;
    }
    return accessToken == other.accessToken &&
        refreshToken == other.refreshToken &&
        tokenType == other.tokenType &&
        user == other.user;
  }

  @override
  int get hashCode => Object.hash(accessToken, refreshToken, tokenType, user);

  @override
  String toString() =>
      'TokenResponse(accessToken=$accessToken, refreshToken=$refreshToken, tokenType=$tokenType, user=$user)';
}
