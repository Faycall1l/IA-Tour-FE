// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'poibriefdistancekm.dart';
import 'poibrieffunfact.dart';
import 'poibrieflatitude.dart';
import 'poibrieflongitude.dart';
import 'poibriefphotourl.dart';
import 'poibriefsubtype.dart';

/// Lightweight POI for list/similar/nearby endpoints — no reviews.
class POIBrief {
  const POIBrief({
    required this.id,
    required this.name,
    required this.category,
    this.subtype,
    required this.wilayaId,
    this.latitude,
    this.longitude,
    this.photoUrl,
    this.isFeatured,
    this.distanceKm,
    this.funFact,
  });

  final String id;
  final String name;
  final String category;
  final POIBriefSubtype? subtype;
  final int wilayaId;
  final POIBriefLatitude? latitude;
  final POIBriefLongitude? longitude;
  final POIBriefPhotoUrl? photoUrl;
  final bool? isFeatured;
  final POIBriefDistanceKm? distanceKm;
  final POIBriefFunFact? funFact;

  /// Creates an instance from a JSON map.
  factory POIBrief.fromJson(Map<String, dynamic> json) {
    return POIBrief(
      id: json['id'] as String,
      name: json['name'] as String,
      category: json['category'] as String,
      subtype: json['subtype'] != null
          ? POIBriefSubtype.fromJson(json['subtype'] as Map<String, dynamic>)
          : null,
      wilayaId: (json['wilaya_id'] as num).toInt(),
      latitude: json['latitude'] != null
          ? POIBriefLatitude.fromJson(json['latitude'] as Map<String, dynamic>)
          : null,
      longitude: json['longitude'] != null
          ? POIBriefLongitude.fromJson(
              json['longitude'] as Map<String, dynamic>)
          : null,
      photoUrl: json['photo_url'] != null
          ? POIBriefPhotoUrl.fromJson(json['photo_url'] as Map<String, dynamic>)
          : null,
      isFeatured:
          json['is_featured'] != null ? json['is_featured'] as bool : null,
      distanceKm: json['distance_km'] != null
          ? POIBriefDistanceKm.fromJson(
              json['distance_km'] as Map<String, dynamic>)
          : null,
      funFact: json['fun_fact'] != null
          ? POIBriefFunFact.fromJson(json['fun_fact'] as Map<String, dynamic>)
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'name': name,
      'category': category,
      if (subtype != null) 'subtype': subtype!?.toJson(),
      'wilaya_id': wilayaId,
      if (latitude != null) 'latitude': latitude!?.toJson(),
      if (longitude != null) 'longitude': longitude!?.toJson(),
      if (photoUrl != null) 'photo_url': photoUrl!?.toJson(),
      if (isFeatured != null) 'is_featured': isFeatured!,
      if (distanceKm != null) 'distance_km': distanceKm!?.toJson(),
      if (funFact != null) 'fun_fact': funFact!?.toJson(),
    };
  }

  POIBrief copyWith({
    String? id,
    String? name,
    String? category,
    POIBriefSubtype? subtype,
    int? wilayaId,
    POIBriefLatitude? latitude,
    POIBriefLongitude? longitude,
    POIBriefPhotoUrl? photoUrl,
    bool? isFeatured,
    POIBriefDistanceKm? distanceKm,
    POIBriefFunFact? funFact,
  }) {
    if (id == null &&
        name == null &&
        category == null &&
        subtype == null &&
        wilayaId == null &&
        latitude == null &&
        longitude == null &&
        photoUrl == null &&
        isFeatured == null &&
        distanceKm == null &&
        funFact == null) {
      return this;
    }

    return POIBrief(
      id: id ?? this.id,
      name: name ?? this.name,
      category: category ?? this.category,
      subtype: subtype ?? this.subtype,
      wilayaId: wilayaId ?? this.wilayaId,
      latitude: latitude ?? this.latitude,
      longitude: longitude ?? this.longitude,
      photoUrl: photoUrl ?? this.photoUrl,
      isFeatured: isFeatured ?? this.isFeatured,
      distanceKm: distanceKm ?? this.distanceKm,
      funFact: funFact ?? this.funFact,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! POIBrief) {
      return false;
    }
    return id == other.id &&
        name == other.name &&
        category == other.category &&
        subtype == other.subtype &&
        wilayaId == other.wilayaId &&
        latitude == other.latitude &&
        longitude == other.longitude &&
        photoUrl == other.photoUrl &&
        isFeatured == other.isFeatured &&
        distanceKm == other.distanceKm &&
        funFact == other.funFact;
  }

  @override
  int get hashCode => Object.hash(id, name, category, subtype, wilayaId,
      latitude, longitude, photoUrl, isFeatured, distanceKm, funFact);

  @override
  String toString() =>
      'POIBrief(id=$id, name=$name, category=$category, subtype=$subtype, wilayaId=$wilayaId, latitude=$latitude, longitude=$longitude, photoUrl=$photoUrl, isFeatured=$isFeatured, distanceKm=$distanceKm, funFact=$funFact)';
}
