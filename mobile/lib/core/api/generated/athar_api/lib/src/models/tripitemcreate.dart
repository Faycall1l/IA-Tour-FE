// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'tripitemcreatetimeslot.dart';

/// Object model for `TripItemCreate` from the SumUp API spec.
class TripItemCreate {
  const TripItemCreate({
    required this.itemType,
    required this.itemId,
    this.dayNumber,
    this.timeSlot,
  });

  final String itemType;
  final String itemId;
  final int? dayNumber;
  final TripItemCreateTimeSlot? timeSlot;

  /// Creates an instance from a JSON map.
  factory TripItemCreate.fromJson(Map<String, dynamic> json) {
    return TripItemCreate(
      itemType: json['item_type'] as String,
      itemId: json['item_id'] as String,
      dayNumber: json['day_number'] != null
          ? (json['day_number'] as num).toInt()
          : null,
      timeSlot: json['time_slot'] != null
          ? TripItemCreateTimeSlot.fromJson(
              json['time_slot'] as Map<String, dynamic>)
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'item_type': itemType,
      'item_id': itemId,
      if (dayNumber != null) 'day_number': dayNumber!,
      if (timeSlot != null) 'time_slot': timeSlot!?.toJson(),
    };
  }

  TripItemCreate copyWith({
    String? itemType,
    String? itemId,
    int? dayNumber,
    TripItemCreateTimeSlot? timeSlot,
  }) {
    if (itemType == null &&
        itemId == null &&
        dayNumber == null &&
        timeSlot == null) {
      return this;
    }

    return TripItemCreate(
      itemType: itemType ?? this.itemType,
      itemId: itemId ?? this.itemId,
      dayNumber: dayNumber ?? this.dayNumber,
      timeSlot: timeSlot ?? this.timeSlot,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! TripItemCreate) {
      return false;
    }
    return itemType == other.itemType &&
        itemId == other.itemId &&
        dayNumber == other.dayNumber &&
        timeSlot == other.timeSlot;
  }

  @override
  int get hashCode => Object.hash(itemType, itemId, dayNumber, timeSlot);

  @override
  String toString() =>
      'TripItemCreate(itemType=$itemType, itemId=$itemId, dayNumber=$dayNumber, timeSlot=$timeSlot)';
}
