// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'discoverexperience.dart';
import 'discoverstay.dart';
import 'guidepoi.dart';
import 'guideresponsecategories.dart';
import 'guideresponsedescription.dart';

/// Object model for `GuideResponse` from the SumUp API spec.
class GuideResponse {
  const GuideResponse({
    required this.wilayaId,
    required this.wilayaName,
    this.description,
    required this.totalPois,
    required this.totalFeatured,
    required this.featuredPois,
    required this.categories,
    required this.experiences,
    required this.stays,
  });

  final int wilayaId;
  final String wilayaName;
  final GuideResponseDescription? description;
  final int totalPois;
  final int totalFeatured;
  final List<GuidePOI> featuredPois;
  final GuideResponseCategories categories;
  final List<DiscoverExperience> experiences;
  final List<DiscoverStay> stays;

  /// Creates an instance from a JSON map.
  factory GuideResponse.fromJson(Map<String, dynamic> json) {
    return GuideResponse(
      wilayaId: (json['wilaya_id'] as num).toInt(),
      wilayaName: json['wilaya_name'] as String,
      description: json['description'] != null
          ? GuideResponseDescription.fromJson(
              json['description'] as Map<String, dynamic>)
          : null,
      totalPois: (json['total_pois'] as num).toInt(),
      totalFeatured: (json['total_featured'] as num).toInt(),
      featuredPois: List<GuidePOI>.generate(
          (json['featured_pois'] as List).length,
          (i) => GuidePOI.fromJson(
              (json['featured_pois'] as List)[i] as Map<String, dynamic>),
          growable: false),
      categories: GuideResponseCategories.fromJson(
          json['categories'] as Map<String, dynamic>),
      experiences: List<DiscoverExperience>.generate(
          (json['experiences'] as List).length,
          (i) => DiscoverExperience.fromJson(
              (json['experiences'] as List)[i] as Map<String, dynamic>),
          growable: false),
      stays: List<DiscoverStay>.generate(
          (json['stays'] as List).length,
          (i) => DiscoverStay.fromJson(
              (json['stays'] as List)[i] as Map<String, dynamic>),
          growable: false),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'wilaya_id': wilayaId,
      'wilaya_name': wilayaName,
      if (description != null) 'description': description!?.toJson(),
      'total_pois': totalPois,
      'total_featured': totalFeatured,
      'featured_pois': featuredPois.map((e) => e?.toJson()).toList(),
      'categories': categories?.toJson(),
      'experiences': experiences.map((e) => e?.toJson()).toList(),
      'stays': stays.map((e) => e?.toJson()).toList(),
    };
  }

  GuideResponse copyWith({
    int? wilayaId,
    String? wilayaName,
    GuideResponseDescription? description,
    int? totalPois,
    int? totalFeatured,
    List<GuidePOI>? featuredPois,
    GuideResponseCategories? categories,
    List<DiscoverExperience>? experiences,
    List<DiscoverStay>? stays,
  }) {
    if (wilayaId == null &&
        wilayaName == null &&
        description == null &&
        totalPois == null &&
        totalFeatured == null &&
        featuredPois == null &&
        categories == null &&
        experiences == null &&
        stays == null) {
      return this;
    }

    return GuideResponse(
      wilayaId: wilayaId ?? this.wilayaId,
      wilayaName: wilayaName ?? this.wilayaName,
      description: description ?? this.description,
      totalPois: totalPois ?? this.totalPois,
      totalFeatured: totalFeatured ?? this.totalFeatured,
      featuredPois: featuredPois ?? this.featuredPois,
      categories: categories ?? this.categories,
      experiences: experiences ?? this.experiences,
      stays: stays ?? this.stays,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! GuideResponse) {
      return false;
    }
    return wilayaId == other.wilayaId &&
        wilayaName == other.wilayaName &&
        description == other.description &&
        totalPois == other.totalPois &&
        totalFeatured == other.totalFeatured &&
        featuredPois == other.featuredPois &&
        categories == other.categories &&
        experiences == other.experiences &&
        stays == other.stays;
  }

  @override
  int get hashCode => Object.hash(wilayaId, wilayaName, description, totalPois,
      totalFeatured, featuredPois, categories, experiences, stays);

  @override
  String toString() =>
      'GuideResponse(wilayaId=$wilayaId, wilayaName=$wilayaName, description=$description, totalPois=$totalPois, totalFeatured=$totalFeatured, featuredPois=$featuredPois, categories=$categories, experiences=$experiences, stays=$stays)';
}
