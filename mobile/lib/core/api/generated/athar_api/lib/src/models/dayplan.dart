// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'tripitemread.dart';

/// Object model for `DayPlan` from the SumUp API spec.
class DayPlan {
  const DayPlan({
    required this.dayNumber,
    required this.items,
    this.totalDistanceKm,
    this.totalCostDzd,
    this.freeSlots,
  });

  final int dayNumber;
  final List<TripItemRead> items;
  final double? totalDistanceKm;
  final double? totalCostDzd;
  final List<String>? freeSlots;

  /// Creates an instance from a JSON map.
  factory DayPlan.fromJson(Map<String, dynamic> json) {
    return DayPlan(
      dayNumber: (json['day_number'] as num).toInt(),
      items: List<TripItemRead>.generate(
          (json['items'] as List).length,
          (i) => TripItemRead.fromJson(
              (json['items'] as List)[i] as Map<String, dynamic>),
          growable: false),
      totalDistanceKm: json['total_distance_km'] != null
          ? (json['total_distance_km'] as num).toDouble()
          : null,
      totalCostDzd: json['total_cost_dzd'] != null
          ? (json['total_cost_dzd'] as num).toDouble()
          : null,
      freeSlots: json['free_slots'] != null
          ? (json['free_slots'] as List).cast<String>()
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'day_number': dayNumber,
      'items': items.map((e) => e?.toJson()).toList(),
      if (totalDistanceKm != null) 'total_distance_km': totalDistanceKm!,
      if (totalCostDzd != null) 'total_cost_dzd': totalCostDzd!,
      if (freeSlots != null) 'free_slots': freeSlots!,
    };
  }

  DayPlan copyWith({
    int? dayNumber,
    List<TripItemRead>? items,
    double? totalDistanceKm,
    double? totalCostDzd,
    List<String>? freeSlots,
  }) {
    if (dayNumber == null &&
        items == null &&
        totalDistanceKm == null &&
        totalCostDzd == null &&
        freeSlots == null) {
      return this;
    }

    return DayPlan(
      dayNumber: dayNumber ?? this.dayNumber,
      items: items ?? this.items,
      totalDistanceKm: totalDistanceKm ?? this.totalDistanceKm,
      totalCostDzd: totalCostDzd ?? this.totalCostDzd,
      freeSlots: freeSlots ?? this.freeSlots,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! DayPlan) {
      return false;
    }
    return dayNumber == other.dayNumber &&
        items == other.items &&
        totalDistanceKm == other.totalDistanceKm &&
        totalCostDzd == other.totalCostDzd &&
        freeSlots == other.freeSlots;
  }

  @override
  int get hashCode =>
      Object.hash(dayNumber, items, totalDistanceKm, totalCostDzd, freeSlots);

  @override
  String toString() =>
      'DayPlan(dayNumber=$dayNumber, items=$items, totalDistanceKm=$totalDistanceKm, totalCostDzd=$totalCostDzd, freeSlots=$freeSlots)';
}
