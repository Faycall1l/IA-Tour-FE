// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'collectionitemread.dart';
import 'collectionreaddescription.dart';
import 'collectionreadupdatedat.dart';

/// Object model for `CollectionRead` from the SumUp API spec.
class CollectionRead {
  const CollectionRead({
    required this.id,
    required this.userId,
    required this.name,
    this.description,
    required this.isPublic,
    this.itemCount,
    required this.createdAt,
    this.updatedAt,
    this.items,
  });

  final String id;
  final String userId;
  final String name;
  final CollectionReadDescription? description;
  final bool isPublic;
  final int? itemCount;
  final DateTime createdAt;
  final CollectionReadUpdatedAt? updatedAt;
  final List<CollectionItemRead>? items;

  /// Creates an instance from a JSON map.
  factory CollectionRead.fromJson(Map<String, dynamic> json) {
    return CollectionRead(
      id: json['id'] as String,
      userId: json['user_id'] as String,
      name: json['name'] as String,
      description: json['description'] != null
          ? CollectionReadDescription.fromJson(
              json['description'] as Map<String, dynamic>)
          : null,
      isPublic: json['is_public'] as bool,
      itemCount: json['item_count'] != null
          ? (json['item_count'] as num).toInt()
          : null,
      createdAt: DateTime.parse(json['created_at'] as String),
      updatedAt: json['updated_at'] != null
          ? CollectionReadUpdatedAt.fromJson(
              json['updated_at'] as Map<String, dynamic>)
          : null,
      items: json['items'] != null
          ? List<CollectionItemRead>.generate(
              (json['items'] as List).length,
              (i) => CollectionItemRead.fromJson(
                  (json['items'] as List)[i] as Map<String, dynamic>),
              growable: false)
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'user_id': userId,
      'name': name,
      if (description != null) 'description': description!?.toJson(),
      'is_public': isPublic,
      if (itemCount != null) 'item_count': itemCount!,
      'created_at': createdAt.toIso8601String(),
      if (updatedAt != null) 'updated_at': updatedAt!?.toJson(),
      if (items != null) 'items': items!.map((e) => e?.toJson()).toList(),
    };
  }

  CollectionRead copyWith({
    String? id,
    String? userId,
    String? name,
    CollectionReadDescription? description,
    bool? isPublic,
    int? itemCount,
    DateTime? createdAt,
    CollectionReadUpdatedAt? updatedAt,
    List<CollectionItemRead>? items,
  }) {
    if (id == null &&
        userId == null &&
        name == null &&
        description == null &&
        isPublic == null &&
        itemCount == null &&
        createdAt == null &&
        updatedAt == null &&
        items == null) {
      return this;
    }

    return CollectionRead(
      id: id ?? this.id,
      userId: userId ?? this.userId,
      name: name ?? this.name,
      description: description ?? this.description,
      isPublic: isPublic ?? this.isPublic,
      itemCount: itemCount ?? this.itemCount,
      createdAt: createdAt ?? this.createdAt,
      updatedAt: updatedAt ?? this.updatedAt,
      items: items ?? this.items,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! CollectionRead) {
      return false;
    }
    return id == other.id &&
        userId == other.userId &&
        name == other.name &&
        description == other.description &&
        isPublic == other.isPublic &&
        itemCount == other.itemCount &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt &&
        items == other.items;
  }

  @override
  int get hashCode => Object.hash(id, userId, name, description, isPublic,
      itemCount, createdAt, updatedAt, items);

  @override
  String toString() =>
      'CollectionRead(id=$id, userId=$userId, name=$name, description=$description, isPublic=$isPublic, itemCount=$itemCount, createdAt=$createdAt, updatedAt=$updatedAt, items=$items)';
}
