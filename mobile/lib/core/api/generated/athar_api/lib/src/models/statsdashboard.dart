// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'categorycount.dart';
import 'wilayacount.dart';

/// Object model for `StatsDashboard` from the SumUp API spec.
class StatsDashboard {
  const StatsDashboard({
    required this.totalPois,
    required this.totalStays,
    required this.totalExperiences,
    required this.totalEvents,
    required this.totalUsers,
    required this.totalTrips,
    required this.poisPerWilaya,
    required this.poisPerCategory,
  });

  final int totalPois;
  final int totalStays;
  final int totalExperiences;
  final int totalEvents;
  final int totalUsers;
  final int totalTrips;
  final List<WilayaCount> poisPerWilaya;
  final List<CategoryCount> poisPerCategory;

  /// Creates an instance from a JSON map.
  factory StatsDashboard.fromJson(Map<String, dynamic> json) {
    return StatsDashboard(
      totalPois: (json['total_pois'] as num).toInt(),
      totalStays: (json['total_stays'] as num).toInt(),
      totalExperiences: (json['total_experiences'] as num).toInt(),
      totalEvents: (json['total_events'] as num).toInt(),
      totalUsers: (json['total_users'] as num).toInt(),
      totalTrips: (json['total_trips'] as num).toInt(),
      poisPerWilaya: List<WilayaCount>.generate(
          (json['pois_per_wilaya'] as List).length,
          (i) => WilayaCount.fromJson(
              (json['pois_per_wilaya'] as List)[i] as Map<String, dynamic>),
          growable: false),
      poisPerCategory: List<CategoryCount>.generate(
          (json['pois_per_category'] as List).length,
          (i) => CategoryCount.fromJson(
              (json['pois_per_category'] as List)[i] as Map<String, dynamic>),
          growable: false),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'total_pois': totalPois,
      'total_stays': totalStays,
      'total_experiences': totalExperiences,
      'total_events': totalEvents,
      'total_users': totalUsers,
      'total_trips': totalTrips,
      'pois_per_wilaya': poisPerWilaya.map((e) => e?.toJson()).toList(),
      'pois_per_category': poisPerCategory.map((e) => e?.toJson()).toList(),
    };
  }

  StatsDashboard copyWith({
    int? totalPois,
    int? totalStays,
    int? totalExperiences,
    int? totalEvents,
    int? totalUsers,
    int? totalTrips,
    List<WilayaCount>? poisPerWilaya,
    List<CategoryCount>? poisPerCategory,
  }) {
    if (totalPois == null &&
        totalStays == null &&
        totalExperiences == null &&
        totalEvents == null &&
        totalUsers == null &&
        totalTrips == null &&
        poisPerWilaya == null &&
        poisPerCategory == null) {
      return this;
    }

    return StatsDashboard(
      totalPois: totalPois ?? this.totalPois,
      totalStays: totalStays ?? this.totalStays,
      totalExperiences: totalExperiences ?? this.totalExperiences,
      totalEvents: totalEvents ?? this.totalEvents,
      totalUsers: totalUsers ?? this.totalUsers,
      totalTrips: totalTrips ?? this.totalTrips,
      poisPerWilaya: poisPerWilaya ?? this.poisPerWilaya,
      poisPerCategory: poisPerCategory ?? this.poisPerCategory,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! StatsDashboard) {
      return false;
    }
    return totalPois == other.totalPois &&
        totalStays == other.totalStays &&
        totalExperiences == other.totalExperiences &&
        totalEvents == other.totalEvents &&
        totalUsers == other.totalUsers &&
        totalTrips == other.totalTrips &&
        poisPerWilaya == other.poisPerWilaya &&
        poisPerCategory == other.poisPerCategory;
  }

  @override
  int get hashCode => Object.hash(totalPois, totalStays, totalExperiences,
      totalEvents, totalUsers, totalTrips, poisPerWilaya, poisPerCategory);

  @override
  String toString() =>
      'StatsDashboard(totalPois=$totalPois, totalStays=$totalStays, totalExperiences=$totalExperiences, totalEvents=$totalEvents, totalUsers=$totalUsers, totalTrips=$totalTrips, poisPerWilaya=$poisPerWilaya, poisPerCategory=$poisPerCategory)';
}
