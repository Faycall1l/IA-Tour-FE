// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'plantriprequestsessionid.dart';

/// Object model for `PlanTripRequest` from the SumUp API spec.
class PlanTripRequest {
  const PlanTripRequest({
    required this.destination,
    required this.durationDays,
    this.budget,
    this.interests,
    this.sessionId,
  });

  final String destination;
  final int durationDays;
  final String? budget;
  final String? interests;
  final PlanTripRequestSessionId? sessionId;

  /// Creates an instance from a JSON map.
  factory PlanTripRequest.fromJson(Map<String, dynamic> json) {
    return PlanTripRequest(
      destination: json['destination'] as String,
      durationDays: (json['duration_days'] as num).toInt(),
      budget: json['budget'] != null ? json['budget'] as String : null,
      interests: json['interests'] != null ? json['interests'] as String : null,
      sessionId: json['session_id'] != null
          ? PlanTripRequestSessionId.fromJson(
              json['session_id'] as Map<String, dynamic>)
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'destination': destination,
      'duration_days': durationDays,
      if (budget != null) 'budget': budget!,
      if (interests != null) 'interests': interests!,
      if (sessionId != null) 'session_id': sessionId!?.toJson(),
    };
  }

  PlanTripRequest copyWith({
    String? destination,
    int? durationDays,
    String? budget,
    String? interests,
    PlanTripRequestSessionId? sessionId,
  }) {
    if (destination == null &&
        durationDays == null &&
        budget == null &&
        interests == null &&
        sessionId == null) {
      return this;
    }

    return PlanTripRequest(
      destination: destination ?? this.destination,
      durationDays: durationDays ?? this.durationDays,
      budget: budget ?? this.budget,
      interests: interests ?? this.interests,
      sessionId: sessionId ?? this.sessionId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! PlanTripRequest) {
      return false;
    }
    return destination == other.destination &&
        durationDays == other.durationDays &&
        budget == other.budget &&
        interests == other.interests &&
        sessionId == other.sessionId;
  }

  @override
  int get hashCode =>
      Object.hash(destination, durationDays, budget, interests, sessionId);

  @override
  String toString() =>
      'PlanTripRequest(destination=$destination, durationDays=$durationDays, budget=$budget, interests=$interests, sessionId=$sessionId)';
}
