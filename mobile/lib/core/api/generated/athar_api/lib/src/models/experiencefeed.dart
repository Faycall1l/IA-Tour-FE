// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'experienceread.dart';

/// Object model for `ExperienceFeed` from the SumUp API spec.
class ExperienceFeed {
  const ExperienceFeed({
    required this.items,
    required this.total,
    required this.page,
    required this.pageSize,
    required this.totalPages,
    required this.hasPrev,
    required this.hasNext,
  });

  final List<ExperienceRead> items;
  final int total;
  final int page;
  final int pageSize;
  final int totalPages;
  final bool hasPrev;
  final bool hasNext;

  /// Creates an instance from a JSON map.
  factory ExperienceFeed.fromJson(Map<String, dynamic> json) {
    return ExperienceFeed(
      items: List<ExperienceRead>.generate(
          (json['items'] as List).length,
          (i) => ExperienceRead.fromJson(
              (json['items'] as List)[i] as Map<String, dynamic>),
          growable: false),
      total: (json['total'] as num).toInt(),
      page: (json['page'] as num).toInt(),
      pageSize: (json['page_size'] as num).toInt(),
      totalPages: (json['total_pages'] as num).toInt(),
      hasPrev: json['has_prev'] as bool,
      hasNext: json['has_next'] as bool,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'items': items.map((e) => e?.toJson()).toList(),
      'total': total,
      'page': page,
      'page_size': pageSize,
      'total_pages': totalPages,
      'has_prev': hasPrev,
      'has_next': hasNext,
    };
  }

  ExperienceFeed copyWith({
    List<ExperienceRead>? items,
    int? total,
    int? page,
    int? pageSize,
    int? totalPages,
    bool? hasPrev,
    bool? hasNext,
  }) {
    if (items == null &&
        total == null &&
        page == null &&
        pageSize == null &&
        totalPages == null &&
        hasPrev == null &&
        hasNext == null) {
      return this;
    }

    return ExperienceFeed(
      items: items ?? this.items,
      total: total ?? this.total,
      page: page ?? this.page,
      pageSize: pageSize ?? this.pageSize,
      totalPages: totalPages ?? this.totalPages,
      hasPrev: hasPrev ?? this.hasPrev,
      hasNext: hasNext ?? this.hasNext,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! ExperienceFeed) {
      return false;
    }
    return items == other.items &&
        total == other.total &&
        page == other.page &&
        pageSize == other.pageSize &&
        totalPages == other.totalPages &&
        hasPrev == other.hasPrev &&
        hasNext == other.hasNext;
  }

  @override
  int get hashCode =>
      Object.hash(items, total, page, pageSize, totalPages, hasPrev, hasNext);

  @override
  String toString() =>
      'ExperienceFeed(items=$items, total=$total, page=$page, pageSize=$pageSize, totalPages=$totalPages, hasPrev=$hasPrev, hasNext=$hasNext)';
}
