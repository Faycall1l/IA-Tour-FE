// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'searchresult.dart';

/// Object model for `SearchFeed` from the SumUp API spec.
class SearchFeed {
  const SearchFeed({
    required this.query,
    required this.results,
    required this.total,
    required this.page,
    required this.pageSize,
    required this.totalPages,
  });

  final String query;
  final List<SearchResult> results;
  final int total;
  final int page;
  final int pageSize;
  final int totalPages;

  /// Creates an instance from a JSON map.
  factory SearchFeed.fromJson(Map<String, dynamic> json) {
    return SearchFeed(
      query: json['query'] as String,
      results: List<SearchResult>.generate(
          (json['results'] as List).length,
          (i) => SearchResult.fromJson(
              (json['results'] as List)[i] as Map<String, dynamic>),
          growable: false),
      total: (json['total'] as num).toInt(),
      page: (json['page'] as num).toInt(),
      pageSize: (json['page_size'] as num).toInt(),
      totalPages: (json['total_pages'] as num).toInt(),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'query': query,
      'results': results.map((e) => e?.toJson()).toList(),
      'total': total,
      'page': page,
      'page_size': pageSize,
      'total_pages': totalPages,
    };
  }

  SearchFeed copyWith({
    String? query,
    List<SearchResult>? results,
    int? total,
    int? page,
    int? pageSize,
    int? totalPages,
  }) {
    if (query == null &&
        results == null &&
        total == null &&
        page == null &&
        pageSize == null &&
        totalPages == null) {
      return this;
    }

    return SearchFeed(
      query: query ?? this.query,
      results: results ?? this.results,
      total: total ?? this.total,
      page: page ?? this.page,
      pageSize: pageSize ?? this.pageSize,
      totalPages: totalPages ?? this.totalPages,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! SearchFeed) {
      return false;
    }
    return query == other.query &&
        results == other.results &&
        total == other.total &&
        page == other.page &&
        pageSize == other.pageSize &&
        totalPages == other.totalPages;
  }

  @override
  int get hashCode =>
      Object.hash(query, results, total, page, pageSize, totalPages);

  @override
  String toString() =>
      'SearchFeed(query=$query, results=$results, total=$total, page=$page, pageSize=$pageSize, totalPages=$totalPages)';
}
