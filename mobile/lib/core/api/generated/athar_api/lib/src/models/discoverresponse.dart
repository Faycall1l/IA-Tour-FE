// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'discoverartisan.dart';
import 'discoverexperience.dart';
import 'discoverpoi.dart';
import 'discoverstay.dart';

/// Object model for `DiscoverResponse` from the SumUp API spec.
class DiscoverResponse {
  const DiscoverResponse({
    required this.wilayaId,
    required this.wilayaName,
    required this.pois,
    required this.experiences,
    required this.stays,
    required this.artisans,
  });

  final int wilayaId;
  final String wilayaName;
  final List<DiscoverPOI> pois;
  final List<DiscoverExperience> experiences;
  final List<DiscoverStay> stays;
  final List<DiscoverArtisan> artisans;

  /// Creates an instance from a JSON map.
  factory DiscoverResponse.fromJson(Map<String, dynamic> json) {
    return DiscoverResponse(
      wilayaId: (json['wilaya_id'] as num).toInt(),
      wilayaName: json['wilaya_name'] as String,
      pois: List<DiscoverPOI>.generate(
          (json['pois'] as List).length,
          (i) => DiscoverPOI.fromJson(
              (json['pois'] as List)[i] as Map<String, dynamic>),
          growable: false),
      experiences: List<DiscoverExperience>.generate(
          (json['experiences'] as List).length,
          (i) => DiscoverExperience.fromJson(
              (json['experiences'] as List)[i] as Map<String, dynamic>),
          growable: false),
      stays: List<DiscoverStay>.generate(
          (json['stays'] as List).length,
          (i) => DiscoverStay.fromJson(
              (json['stays'] as List)[i] as Map<String, dynamic>),
          growable: false),
      artisans: List<DiscoverArtisan>.generate(
          (json['artisans'] as List).length,
          (i) => DiscoverArtisan.fromJson(
              (json['artisans'] as List)[i] as Map<String, dynamic>),
          growable: false),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'wilaya_id': wilayaId,
      'wilaya_name': wilayaName,
      'pois': pois.map((e) => e?.toJson()).toList(),
      'experiences': experiences.map((e) => e?.toJson()).toList(),
      'stays': stays.map((e) => e?.toJson()).toList(),
      'artisans': artisans.map((e) => e?.toJson()).toList(),
    };
  }

  DiscoverResponse copyWith({
    int? wilayaId,
    String? wilayaName,
    List<DiscoverPOI>? pois,
    List<DiscoverExperience>? experiences,
    List<DiscoverStay>? stays,
    List<DiscoverArtisan>? artisans,
  }) {
    if (wilayaId == null &&
        wilayaName == null &&
        pois == null &&
        experiences == null &&
        stays == null &&
        artisans == null) {
      return this;
    }

    return DiscoverResponse(
      wilayaId: wilayaId ?? this.wilayaId,
      wilayaName: wilayaName ?? this.wilayaName,
      pois: pois ?? this.pois,
      experiences: experiences ?? this.experiences,
      stays: stays ?? this.stays,
      artisans: artisans ?? this.artisans,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! DiscoverResponse) {
      return false;
    }
    return wilayaId == other.wilayaId &&
        wilayaName == other.wilayaName &&
        pois == other.pois &&
        experiences == other.experiences &&
        stays == other.stays &&
        artisans == other.artisans;
  }

  @override
  int get hashCode =>
      Object.hash(wilayaId, wilayaName, pois, experiences, stays, artisans);

  @override
  String toString() =>
      'DiscoverResponse(wilayaId=$wilayaId, wilayaName=$wilayaName, pois=$pois, experiences=$experiences, stays=$stays, artisans=$artisans)';
}
