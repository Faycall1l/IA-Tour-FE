// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'eventreaddescription.dart';
import 'eventreaddurationdays.dart';
import 'eventreadisrecurring.dart';
import 'eventreadphotourl.dart';
import 'eventreadupdatedat.dart';

/// Object model for `EventRead` from the SumUp API spec.
class EventRead {
  const EventRead({
    required this.id,
    required this.title,
    required this.wilayaId,
    required this.category,
    required this.description,
    required this.month,
    required this.durationDays,
    required this.isRecurring,
    required this.photoUrl,
    required this.createdAt,
    required this.updatedAt,
  });

  final String id;
  final String title;
  final int wilayaId;
  final String category;
  final EventReadDescription? description;
  final int month;
  final EventReadDurationDays? durationDays;
  final EventReadIsRecurring? isRecurring;
  final EventReadPhotoUrl? photoUrl;
  final DateTime createdAt;
  final EventReadUpdatedAt? updatedAt;

  /// Creates an instance from a JSON map.
  factory EventRead.fromJson(Map<String, dynamic> json) {
    return EventRead(
      id: json['id'] as String,
      title: json['title'] as String,
      wilayaId: (json['wilaya_id'] as num).toInt(),
      category: json['category'] as String,
      description: EventReadDescription.fromJson(
          json['description'] as Map<String, dynamic>),
      month: (json['month'] as num).toInt(),
      durationDays: EventReadDurationDays.fromJson(
          json['duration_days'] as Map<String, dynamic>),
      isRecurring: EventReadIsRecurring.fromJson(
          json['is_recurring'] as Map<String, dynamic>),
      photoUrl:
          EventReadPhotoUrl.fromJson(json['photo_url'] as Map<String, dynamic>),
      createdAt: DateTime.parse(json['created_at'] as String),
      updatedAt: EventReadUpdatedAt.fromJson(
          json['updated_at'] as Map<String, dynamic>),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'title': title,
      'wilaya_id': wilayaId,
      'category': category,
      'description': description?.toJson(),
      'month': month,
      'duration_days': durationDays?.toJson(),
      'is_recurring': isRecurring?.toJson(),
      'photo_url': photoUrl?.toJson(),
      'created_at': createdAt.toIso8601String(),
      'updated_at': updatedAt?.toJson(),
    };
  }

  EventRead copyWith({
    String? id,
    String? title,
    int? wilayaId,
    String? category,
    EventReadDescription? description,
    int? month,
    EventReadDurationDays? durationDays,
    EventReadIsRecurring? isRecurring,
    EventReadPhotoUrl? photoUrl,
    DateTime? createdAt,
    EventReadUpdatedAt? updatedAt,
  }) {
    if (id == null &&
        title == null &&
        wilayaId == null &&
        category == null &&
        description == null &&
        month == null &&
        durationDays == null &&
        isRecurring == null &&
        photoUrl == null &&
        createdAt == null &&
        updatedAt == null) {
      return this;
    }

    return EventRead(
      id: id ?? this.id,
      title: title ?? this.title,
      wilayaId: wilayaId ?? this.wilayaId,
      category: category ?? this.category,
      description: description ?? this.description,
      month: month ?? this.month,
      durationDays: durationDays ?? this.durationDays,
      isRecurring: isRecurring ?? this.isRecurring,
      photoUrl: photoUrl ?? this.photoUrl,
      createdAt: createdAt ?? this.createdAt,
      updatedAt: updatedAt ?? this.updatedAt,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! EventRead) {
      return false;
    }
    return id == other.id &&
        title == other.title &&
        wilayaId == other.wilayaId &&
        category == other.category &&
        description == other.description &&
        month == other.month &&
        durationDays == other.durationDays &&
        isRecurring == other.isRecurring &&
        photoUrl == other.photoUrl &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hash(id, title, wilayaId, category, description,
      month, durationDays, isRecurring, photoUrl, createdAt, updatedAt);

  @override
  String toString() =>
      'EventRead(id=$id, title=$title, wilayaId=$wilayaId, category=$category, description=$description, month=$month, durationDays=$durationDays, isRecurring=$isRecurring, photoUrl=$photoUrl, createdAt=$createdAt, updatedAt=$updatedAt)';
}
