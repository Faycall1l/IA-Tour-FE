// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'discoverexperiencedescription.dart';
import 'discoverexperiencedurationhours.dart';
import 'discoverexperiencemeetingpoint.dart';
import 'discoverexperiencephotos.dart';
import 'discoverexperiencepricedzd.dart';
import 'discoverexperienceprovideravatar.dart';
import 'discoverexperienceprovidername.dart';

/// Object model for `DiscoverExperience` from the SumUp API spec.
class DiscoverExperience {
  const DiscoverExperience({
    required this.id,
    required this.title,
    required this.category,
    required this.description,
    required this.priceDzd,
    required this.durationHours,
    required this.providerName,
    required this.providerAvatar,
    required this.meetingPoint,
    required this.photos,
  });

  final String id;
  final String title;
  final String category;
  final DiscoverExperienceDescription? description;
  final DiscoverExperiencePriceDzd? priceDzd;
  final DiscoverExperienceDurationHours? durationHours;
  final DiscoverExperienceProviderName? providerName;
  final DiscoverExperienceProviderAvatar? providerAvatar;
  final DiscoverExperienceMeetingPoint? meetingPoint;
  final DiscoverExperiencePhotos? photos;

  /// Creates an instance from a JSON map.
  factory DiscoverExperience.fromJson(Map<String, dynamic> json) {
    return DiscoverExperience(
      id: json['id'] as String,
      title: json['title'] as String,
      category: json['category'] as String,
      description: DiscoverExperienceDescription.fromJson(
          json['description'] as Map<String, dynamic>),
      priceDzd: DiscoverExperiencePriceDzd.fromJson(
          json['price_dzd'] as Map<String, dynamic>),
      durationHours: DiscoverExperienceDurationHours.fromJson(
          json['duration_hours'] as Map<String, dynamic>),
      providerName: DiscoverExperienceProviderName.fromJson(
          json['provider_name'] as Map<String, dynamic>),
      providerAvatar: DiscoverExperienceProviderAvatar.fromJson(
          json['provider_avatar'] as Map<String, dynamic>),
      meetingPoint: DiscoverExperienceMeetingPoint.fromJson(
          json['meeting_point'] as Map<String, dynamic>),
      photos: DiscoverExperiencePhotos.fromJson(
          json['photos'] as Map<String, dynamic>),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'title': title,
      'category': category,
      'description': description?.toJson(),
      'price_dzd': priceDzd?.toJson(),
      'duration_hours': durationHours?.toJson(),
      'provider_name': providerName?.toJson(),
      'provider_avatar': providerAvatar?.toJson(),
      'meeting_point': meetingPoint?.toJson(),
      'photos': photos?.toJson(),
    };
  }

  DiscoverExperience copyWith({
    String? id,
    String? title,
    String? category,
    DiscoverExperienceDescription? description,
    DiscoverExperiencePriceDzd? priceDzd,
    DiscoverExperienceDurationHours? durationHours,
    DiscoverExperienceProviderName? providerName,
    DiscoverExperienceProviderAvatar? providerAvatar,
    DiscoverExperienceMeetingPoint? meetingPoint,
    DiscoverExperiencePhotos? photos,
  }) {
    if (id == null &&
        title == null &&
        category == null &&
        description == null &&
        priceDzd == null &&
        durationHours == null &&
        providerName == null &&
        providerAvatar == null &&
        meetingPoint == null &&
        photos == null) {
      return this;
    }

    return DiscoverExperience(
      id: id ?? this.id,
      title: title ?? this.title,
      category: category ?? this.category,
      description: description ?? this.description,
      priceDzd: priceDzd ?? this.priceDzd,
      durationHours: durationHours ?? this.durationHours,
      providerName: providerName ?? this.providerName,
      providerAvatar: providerAvatar ?? this.providerAvatar,
      meetingPoint: meetingPoint ?? this.meetingPoint,
      photos: photos ?? this.photos,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! DiscoverExperience) {
      return false;
    }
    return id == other.id &&
        title == other.title &&
        category == other.category &&
        description == other.description &&
        priceDzd == other.priceDzd &&
        durationHours == other.durationHours &&
        providerName == other.providerName &&
        providerAvatar == other.providerAvatar &&
        meetingPoint == other.meetingPoint &&
        photos == other.photos;
  }

  @override
  int get hashCode => Object.hash(id, title, category, description, priceDzd,
      durationHours, providerName, providerAvatar, meetingPoint, photos);

  @override
  String toString() =>
      'DiscoverExperience(id=$id, title=$title, category=$category, description=$description, priceDzd=$priceDzd, durationHours=$durationHours, providerName=$providerName, providerAvatar=$providerAvatar, meetingPoint=$meetingPoint, photos=$photos)';
}
