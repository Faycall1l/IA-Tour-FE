// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'userreadavatarurl.dart';
import 'userreadbio.dart';
import 'userreaddisplayname.dart';
import 'userreadlanguages.dart';

/// Object model for `UserRead` from the SumUp API spec.
class UserRead {
  const UserRead({
    required this.id,
    required this.phone,
    required this.role,
    required this.language,
    required this.isActive,
    required this.isVerified,
    required this.displayName,
    required this.avatarUrl,
    required this.languages,
    required this.bio,
    required this.createdAt,
  });

  final String id;
  final String phone;
  final String role;
  final String language;
  final bool isActive;
  final bool isVerified;
  final UserReadDisplayName? displayName;
  final UserReadAvatarUrl? avatarUrl;
  final UserReadLanguages? languages;
  final UserReadBio? bio;
  final DateTime createdAt;

  /// Creates an instance from a JSON map.
  factory UserRead.fromJson(Map<String, dynamic> json) {
    return UserRead(
      id: json['id'] as String,
      phone: json['phone'] as String,
      role: json['role'] as String,
      language: json['language'] as String,
      isActive: json['is_active'] as bool,
      isVerified: json['is_verified'] as bool,
      displayName: UserReadDisplayName.fromJson(
          json['display_name'] as Map<String, dynamic>),
      avatarUrl: UserReadAvatarUrl.fromJson(
          json['avatar_url'] as Map<String, dynamic>),
      languages:
          UserReadLanguages.fromJson(json['languages'] as Map<String, dynamic>),
      bio: UserReadBio.fromJson(json['bio'] as Map<String, dynamic>),
      createdAt: DateTime.parse(json['created_at'] as String),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'phone': phone,
      'role': role,
      'language': language,
      'is_active': isActive,
      'is_verified': isVerified,
      'display_name': displayName?.toJson(),
      'avatar_url': avatarUrl?.toJson(),
      'languages': languages?.toJson(),
      'bio': bio?.toJson(),
      'created_at': createdAt.toIso8601String(),
    };
  }

  UserRead copyWith({
    String? id,
    String? phone,
    String? role,
    String? language,
    bool? isActive,
    bool? isVerified,
    UserReadDisplayName? displayName,
    UserReadAvatarUrl? avatarUrl,
    UserReadLanguages? languages,
    UserReadBio? bio,
    DateTime? createdAt,
  }) {
    if (id == null &&
        phone == null &&
        role == null &&
        language == null &&
        isActive == null &&
        isVerified == null &&
        displayName == null &&
        avatarUrl == null &&
        languages == null &&
        bio == null &&
        createdAt == null) {
      return this;
    }

    return UserRead(
      id: id ?? this.id,
      phone: phone ?? this.phone,
      role: role ?? this.role,
      language: language ?? this.language,
      isActive: isActive ?? this.isActive,
      isVerified: isVerified ?? this.isVerified,
      displayName: displayName ?? this.displayName,
      avatarUrl: avatarUrl ?? this.avatarUrl,
      languages: languages ?? this.languages,
      bio: bio ?? this.bio,
      createdAt: createdAt ?? this.createdAt,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! UserRead) {
      return false;
    }
    return id == other.id &&
        phone == other.phone &&
        role == other.role &&
        language == other.language &&
        isActive == other.isActive &&
        isVerified == other.isVerified &&
        displayName == other.displayName &&
        avatarUrl == other.avatarUrl &&
        languages == other.languages &&
        bio == other.bio &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hash(id, phone, role, language, isActive,
      isVerified, displayName, avatarUrl, languages, bio, createdAt);

  @override
  String toString() =>
      'UserRead(id=$id, phone=$phone, role=$role, language=$language, isActive=$isActive, isVerified=$isVerified, displayName=$displayName, avatarUrl=$avatarUrl, languages=$languages, bio=$bio, createdAt=$createdAt)';
}
